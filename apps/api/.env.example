# Required for web UI reviews (optional if using CLI pipeline)
ANTHROPIC_API_KEY=

# Database (defaults to postgres://localhost:5432/committee_orchestrator)
DATABASE_URL=

# Optional: Etherscan API key for on-chain data
ETHERSCAN_API_KEY=
